//numéro3

let n = Number;
let m = Number;
if (n > m)  (prompt(" afficher un message d'erreur et arrêter le programme:"));

 let n = Number;
 let m = Number;
    m = m + n
    n = m - n
    m = m - n

let m = prompt ("m=");
 let n = prompt ("n=");
m = m * 1;
n = n * 1;
console.log (typeof m);
console.log (typeof n);
if (NaN (m)) || is NaN(n){
    console.log) ("attention à votre saise");
}else if (n>m){
    console.log ("n doit être supérieur à m");
}
for  (let i =n ; i>=m; i=i-1);

let somme = 0;
for (let i = n); i>=m; i =i-1){
    console .log (i);
}

//numéro4

let compteur = 10
do {
    console.log(compteur);
    compteur = compteur + 1;
}while (compteur <= 10)

let compteur = 10 (prompt("le plus grand nombre ");



